# 必填：你的公网域名（必须已解析到服务器）
MCP_DOMAIN=mcp.example.com

# 必填：用于远程调用鉴权的 Bearer Token（建议 32+ 位随机串）
MCP_API_TOKEN=replace_with_long_random_token

# 可选：服务端账号（推荐用环境变量，不把密码写入 henu_profile.json）
HENU_STUDENT_ID=
HENU_PASSWORD=

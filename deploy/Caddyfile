{$MCP_DOMAIN:localhost} {
    encode gzip

    @mcp path /mcp*
    @unauth {
        path /mcp*
        not header Authorization "Bearer {$MCP_API_TOKEN}"
    }

    respond @unauth "Unauthorized" 401
    reverse_proxy @mcp henu-mcp:18001

    respond "HENU MCP is running" 200
}
